FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install

COPY . .

EXPOSE 5173

# Expose host so the container port is reachable from the host
CMD ["npx", "vite", "--host", "0.0.0.0", "--port", "5173"]
